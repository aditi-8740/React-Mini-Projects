import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// const myfirstElement = <h1>hello bachoo</h1>
// root.render(myfirstElement)   //App Comp. dekhaoo..Aur kaha dekhana ha?root pr mean root element in index.html
